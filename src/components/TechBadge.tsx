export const TechBadge = ({ label }: { label: string }) => (
    <span className="px-2 py-0.5 rounded-full bg-white/5 border border-white/10 text-[9px] mono uppercase tracking-wider text-white/40">
      {label}
    </span>
  );